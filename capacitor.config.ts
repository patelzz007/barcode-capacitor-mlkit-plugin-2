import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barcode.mlkit',
  appName: 'barcode-mlkit-2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
